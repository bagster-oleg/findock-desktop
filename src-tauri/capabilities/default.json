{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Окно кабинета. Из страницы никаких вызовов в приложение не делается — минимум прав.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
